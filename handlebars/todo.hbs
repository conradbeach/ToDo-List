<li class="{{ternary completed 'complete' ''}}">
  <span>
    <strong>{{title}}</strong>
    {{#if description}}
      : {{description}}
    {{/if}}

    {{#if dueDate}}
      <time>{{formatDate dueDate}}</time>
    {{/if}}
  </span>
  <a href="#"></a>
</li>
